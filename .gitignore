node_modules/
.next/
out/
build/
dist/
.turbo/
*.tsbuildinfo

# Written by `npm run seo:report` — a build artefact, never a source of truth.
seo-report.json
next-env.d.ts
.DS_Store
.vercel

# env files — the template carries no secrets and MUST be committed
.env*
!.env.example

# Regenerated by `next dev` on every run.
apps/web/AGENTS.md
apps/web/CLAUDE.md
